{
  "name": "convolution-example",
  "version": "1.0.0",
  "description": "You must have node.js installed.",
  "main": "index.js",
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html --no-minify --public-url https://mathisonian.github.io/p5-convolution-example/; rm -rf docs; mv dist docs;",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "p5": "^1.0.0",
    "parcel": "^1.12.4"
  }
}
